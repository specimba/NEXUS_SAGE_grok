# P5 Shell polish — ops console not 90s frontpage

Canberk: dashboard looked like 90s frontpage.

Shipped (token freeze + Skin V2 vibe):
- desk-shell phosphor grid ground
- sticky topbar + status chips (STABLE / PULSE LIVE|STALE)
- segmented desk-lane tabs with `[01]` amber prefixes + block cursor on active
- desk-stage framed console + `> lane · …` rail + ≤8% `.crt-rain` (off under reduced-motion)
- amber corner ticks on green `.sage-ticks` panels (4-corner L-marks, not wallpaper)
- pin TAKE/WHY/MOVE as dl.pin-meta; `>` kickers on headers
- denser gaps (instrument console, not stacked frontpage)
- max-w-7xl ops board

Locks: `data-theme="phosphor"` · green `#33ff66` primary · amber scarce · no cyan/magenta fills.
