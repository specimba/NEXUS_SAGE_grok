# Visual QA — 2026-09-04 (UX Gürok)

**Target:** live desk after Skin V2 + CRT density + ingest stamp  
**URL:** `http://127.0.0.1:3000/` → HTTP 200  
**Locks:** `003` / `hf-incident` · phosphor primary · free providers · no token churn this pass

## Passes
| Check | Result |
|-------|--------|
| `data-theme="phosphor"` | PASS |
| Primary `#33ff66` | PASS |
| Amber scarce (`--pip-amber` ticks/companion only) | PASS — not wallpaper |
| Holo 1px only / no cyan fills | PASS |
| Ops shell (grid + sticky topbar + segmented lanes) | PASS — not 90s frontpage |
| Block cursor (brand + active lane) | PASS |
| `[01]`…`[06]` amber lane prefixes + `> lane` rail | PASS |
| Stage rain opacity `0.08` + reduced-motion kill | PASS |
| All lane panels `.sage-panel` + `.sage-ticks` | PASS |
| brand-check | 0 warnings |
| Cycle lock in Brief data | `003` / `hf-incident` present |

## Regressions / nits (non-blocking)
1. **Lead pin inverse** — `sage-lead` appears on the kind label / conditional class; confirm full card still reads as inverse black-on-phosphor (not just the word “lead”). Cosmetic if glow-only.
2. **CRT scanline opacity** currently `0.18` (within Skin V2 0.12–0.18 band). Fine; don’t raise above 0.18.
3. **Duplicate `.crt-rain` rules** in `globals.css` (two blocks). Harmless but Coder can dedupe on next CSS touch.

## Verdict
**Visual QA PASS** for ops-console vibe. No token churn. No FAIL that blocks arXiv/Papers enrich.

## Fix applied during QA
- Restored \`sage-lead\` on lead pin **card** (was demoted to label-only).

## Lead-pin inverse — closed
- Full lead **card** uses `.sage-lead` (phosphor fill, dark type, amber inset tick).
- Legend/kind chip uses `.sage-lead-badge` (label-only).
- No token churn.
