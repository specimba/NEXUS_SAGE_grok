# Brief polish (UX) — 2026-09-04
- CRT grain: true `--crt-scanline-opacity: 0.18` (removed stacked `opacity-40`)
- Take panel: `sage-panel` + glow; exec lines with hairline rules
- Pin legend: header chrome + square swatches (lead=signal, companion=amber, rest=dim)
- Lead pin: `sage-panel-glow`
- Wave footer: relative amber bars (brightness hierarchy, no new hexes); METR/OpenAI split unchanged
- ALLOW/DENY/Open strip: untouched (already correct)
- Tokens: frozen
